"""Update workflow."""

from .utils import info


def run_update(args):
    """Update launcher, winetricks, DXVK, and UI fixes."""
    info("Update workflow coming in V2.0 — use install.sh for now.")
